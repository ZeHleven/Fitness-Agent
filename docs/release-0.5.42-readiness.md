# 小程序 0.5.42 发布范围与交付约定

日期：2026-09-11。用户已验收本轮隔离版并授权提交与发布。

## 发布范围

- 小程序从 0.5.41 升级至 0.5.42；后端保留 0.5.41，数据库保留 0029。本次无后端部署包和迁移任务。
- 包含此前已验收的训练保存反馈、休息面板动效/生命周期、冗余结束入口精简，自定义动作 B 抽屉、真实改动离页提醒和深绿原生导航修复。
- 包含本次训练历史紧凑摘要、独立提案入口、计划组次摘要及整卡开合、未训练动作汇总、行内组间歇展示。
- 不修改 Agent、模型、饮食生成、训练写入/计时/热量规则、Proposal 授权协议，不扩展动作库或食品库，不引入新的运行依赖。

## 已有验证

- 第一阶段与抽屉分组验收见相应阶段文档；历史交互用户整体反馈“验收通过”。不将用户反馈扩大为所有机型/网络/字体/性能矩阵通过。
- 当前全部小程序测试 290 项（含 Proposal 和行为契约）、类型检查、生产模式隔离构建与全包 25 个 JavaScript 文件的 ES2019 语法门禁通过。
- 新旧测试包各自冻结；最后隔离版为 `v0.5.42 · c74bd95d0cea · dirty`，其 miniapp 源码指纹 `194acfb80dc8c4c99072519dace643aff1d053f4b0913531adfc11181345ee0d`，产物指纹 `8b820daebd65c27d003c8211a8a2965eabf945193535b410b834ff0da9a80062`，94 文件、648916 字节。
- 渲染检查仅使用实际 Taro H5 构建、合成数据和宿主默认样式适配；不把它表述为微信真机 FPS、原生截图或真实网络性能。

## 发布门禁

1. 提交前校验候选文件、源码凭证扫描、全量小程序测试与类型检查。只提交本轮源码、测试和文档，不提交测试配置、账号、数据库、预览产物或 .env。
2. 推送专用分支并创建 PR；等待标准 Project CI 两项任务成功（后端测试/确定性 Agent 评测/迁移/容器构建、小程序契约/类型/构建），不跳过失败项或保护规则。
3. 合并后从与远程 main 完全一致的干净 Git 提交构建正式小程序，核对完整 SHA、版本及 `source_dirty=false`。
4. 正式包恢复既有微信云托管连接、清除局域网测试地址、开启合法域名校验、关闭热重载，检查正式服务健康。保留上一版正式包和所有隔离包，创建新目录交付。
5. 正式包仍需用户扫码预览，核对版本/登录、训练历史/详情、抽屉/导航和核心流程，然后由用户手动上传。上传成功不等同于已审核或公开上线。

实际 PR、CI、合并 SHA、产物清单与健康核对结果记入正式产物旁的发布说明，不向旧包回填新元数据。本次不新增真实模型发布门禁。
