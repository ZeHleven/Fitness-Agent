# 小程序 0.5.35：三项已确认界面修复

日期：2026-09-08。基线：`7958ef2a4b56a25d92376870dfa0b4f5b3a9e6cc`。

## 范围

- 修复自定义动作名称、训练部位、已知禁忌的单行文字裁切。编辑器和个性化计划生成页共用同一组件。
- 归档计划标题右侧按“删除 → 已归档”排列；两者共用同一标签框尺寸，同高、同圆角、同内边距，文字上下居中且外框上下边缘对齐，宽度随文字贴合。按用户最新要求，“删除”复用“删除计划”的红字 `#9d382b` 和浅红底框 `#fff0ed`；“已归档”保持灰色。外围保持透明点击区域；保留页内确认、取消和失败重试。
- “已完成 · 查看”改为“已完成”，仍打开训练详情，不重新开始；辅助阅读标签仍说明查看详情及对应星期。
- 仅小程序版本改为 `0.5.35`；后端 `0.5.34`、数据库 `0027`、健康校验、未知安全信息提示、API 与写入流程不变。

## 验证

- 先补回归测试：定向 12 项中 5 项失败，分别揭示缺失的输入样式、标题操作位置、按钮文案及尺寸。完成实现后通过。
- 小程序全部 124 项行为与契约测试通过；此前 TypeScript 检查通过，本次只改配色，生产模式构建和产物契约再次通过。
- 活动及归档计划中的“已完成”点击均只导航到详情，零开始训练调用；取消归档删除零写入，删除失败保留确认状态，重试成功才更新列表。
- 使用实际 TSX 树、编译后的 Sass 和模拟 rpx 缩放，在本地 Chromium 检查 320、375、430 宽度及 1.3 倍字体；六组均未检测到横向溢出，输入内容高度充足，删除标签与归档标签同排相邻，框的上下边缘及圆角、内边距、字号、字重、行高一致；删除配色与“删除计划”一致。已人工查看生成截图。
- 上述浏览器渲染是布局近似检查，不等同微信模拟器或 iPhone 原生输入控件验收。本次没有自动操作用户数据，也没有运行真实模型门禁。
- 2026-09-08 用户反馈“修复都已通过”，据此记录本次三项修复及后续对齐、红色配色调整的真机复验通过；这是用户反馈，不是代理直接操作真机的结果。

## 本地测试构建

- 导入目录：`.tmp/training-ui-0.5.35-build/dist`。
- 标识：`v0.5.35 · 7958ef2a4b56 · dirty`。这是未提交源码的独立测试构建，`dirty=true` 如实保留，不是正式发布包。
- `miniapp/dist` 及 `deploy/cloudbase/0.5.34-7958ef2/miniapp-dist` 均未覆盖；后者继续作为已验收正式版本与回退来源。
- 本次未提交、推送、创建 PR、运行远程 CI、上传或部署。测试构建已获用户验收；正式发布仍需授权进入提交流程，经 CI、合并和干净构建，核对正式包标识并扫码冒烟后交付。

## 定向真机复验

1. 两个自定义动作入口：输入名称和部位、检查禁忌占位文字及光标；长文字左右移动编辑，弹出/收起键盘后不裁切；原有安全小字仍显示。
2. 归档卡片：“删除”紧邻“已归档”左侧，可见底框不再有大块空白；长计划名不重叠；先点删除再点保留，卡片仍在；需要实际删除时只用测试计划，确认后训练历史仍可找回。
3. 活动及归档计划：已完成日只显示“已完成”，点击进入正确训练详情，未完成日行为保持不变。

真机状态：用户于 2026-09-08 对本次修复整体反馈通过。未将该反馈扩展为所有机型、字体设置或此前条件性边界场景已逐项通过，也不代表正式发布已经完成。
