# 食品库扩充与包装录入：实现交接

## 状态与隔离

2026-09-13；codex/food-library-expansion，基线
5965fb56c3233e9883878c7006d217b5e4ba173b。
后端／小程序0.5.45，未提交dirty构建，正式0.5.44不变。
仅本机 PostgreSQL：fitness_food_0545_tests、fitness_food_0545_migration、
fitness_food_0545_acceptance；未读取或写入生产数据库，不调用真实模型。
未推送／PR／部署／上传。正式发布仍需另行授权。

## 实现

- 0031接0030；59新增＋21原有＝80。旧食品ID／营养／Food.category不重写。
- 独立browse_category、非唯一浏览别名；8分类、12项每页，额外取1项判断下一页；搜索防抖、乱序隔离、离页取消后返回恢复。
- 原GET /foods仍是公共库，原过滤与Agent精确别名解析不变；增量source_info保留出处。
- GET /foods/library增量分类和offset；私人食品仍仅本人可见，不进入公共库。
- 导入台账和确定性ID：重复不新增、不覆盖；同名冲突拒绝。降级若有营养／元数据更改或餐次／Agent引用则拒绝，不静默删除。
- 私人表单仅每100g营养，默认kJ（除4.184），支持kcal。实际克数仅换算预览／草稿；明确零与空白不同。异常营养只提示核对，不以4/4/9改写。
- 旧basis非100g在编辑时按API归一化字段展示；打开不写回，修改不改变历史餐次或当前草稿。
- 创建响应不确定时先通过本人request_id读取；旧请求成功但输入变更时保留新输入并提示，不盲目再次POST。餐次保存仍由服务端批量重算和版本校验。
- 各来源原始字段与生熟定义保存在v3矩阵；AFCD专用许可、完整限制说明、地区适用性及变更声明跟随数据/API。backend/app/data/FOOD-DATA-NOTICES.md随后端包分发。

## 已执行验证

- 完整后端：1126 passed；后续ready检查／发布包清单的定向回归51 passed。
- 本地0031→0023→0031成功；0031单独升降级、引用／修改拒绝降级、冲突回滚测试通过。
- 确定性Agent：39/39，strict通过，无真实模型请求。
- 小程序类型检查、263项构建／行为契约、88项Proposal契约通过。
- 生产模式weapp构建与全包语法门禁通过：26个JavaScript文件。
- 公共候选凭据扫描通过。59项源数据通过冻结生成器导入，不由模型生成数值。
- 实际本地接口核对80项、8类、分页不重复、土豆／紫薯双状态、AFCD出处随响应返回。
- 实际H5组件连接隔离后端补测输入／搜索／保存到草稿。320／393px有截图记录；H5原生组件样式与微信不同，不能当作微信视觉或键盘验收通过。
- 本机查询样本25次，每页3个SQL，首次36.43ms，热态中位4.59ms，P95约5.63ms；不代表生产网络耗时。
- 后端依赖不变。未压缩JS合计549583字节，相对正式0.5.44的542079字节增加7504字节（约7.3KiB）。包体和文件SHA256记录于隔离包manifest.json。

## 交付与下一步

验收结论（2026-09-14）：用户明确“食品库验收通过”。下一步可在另获授权后进入0.5.45提交与发布流程，复核最终改动、Project CI、0031发布/回退保护和正式构建来源。Agent真实调用未验收；当前隔离环境模型和worker关闭，入队等待问题是已说明但未修改的测试环境限制。发布核对需避免将隔离开关、地址、账号或启动脚本带入正式环境。

2026-09-14验收微调：“保存修改／取消编辑”改成等宽同一行，字号26rpx、最小高度80rpx；仅编辑既有餐次生效，新建餐次布局和全部保存/取消逻辑不变。36项饮食行为测试、类型检查、weapp构建及26个JS门禁通过。已更新现有r2/miniapp-dist，旧包和旧manifest保存在r2目录外层的before-compact-actions备份。

最新：2026-09-14因换网及进程停止，已恢复至
.tmp/food-library-test-20260914-r2/miniapp-dist，API192.168.0.7:8556；
原隔离库数据保留，无重复迁移或初始化。r2构建／冻结包校验通过；下述r1为首次交付历史。

独立目录：工作区 .tmp/food-library-test-20260913-r1/miniapp-dist。
局域网后端192.168.1.21:8556，数据库0031，禁用模型；/ready及/test-info可核对。
完整后端源快照、每文件哈希及测试记录保存在同级目录；不得将此包上传为正式版本。
验收范围：分类／生熟搜索、每100g与kJ换算、旧食品编辑和历史快照、失败重试／草稿、真机小屏及键盘。
GitHub Project CI／Python3.13容器构建留在授权提交发布阶段，未声称远端CI已通过。
