# 0.5.45 发布准备：食品库扩充与包装食品录入

2026-09-14。用户已确认食品库隔离验收通过，并要求进入提交与发布流程。
本地提交准备已获授权；远端推送／PR和合并按后续确认执行。
生产部署、数据库迁移开关和小程序上传仍由用户手动执行。

## 版本与范围

- 分支codex/food-library-expansion；main基线5965fb56c3233e9883878c7006d217b5e4ba173b，发布准备时已与GitHub main只读核对一致。
- 后端、小程序0.5.44 → 0.5.45；数据库0030 → 0031。
- 59项来源核验后的公共食品＋21项既有定义，8个浏览分类、别名搜索与分页。土豆／紫薯保留生／蒸熟区分。
- 包装食品使用每100g营养＋本次克数，kJ/kcal换算；私人库、餐次草稿和最终保存分开。旧basis编辑归一化，历史快照不变，失败／版本冲突保留输入。
- 编辑餐次的保存修改／取消编辑已按用户要求缩小并排，未改变新建餐次或提案确认流程。
- 未扩展有氧、OCR、扫码或外部营养API，未改模型、Agent意图／生成／Proposal链路。

## 门禁与交付约束

- 最终本地后端全测、迁移升降级保护、Agent确定性39项、小程序264项构建／行为契约、88项Proposal契约、类型检查、生产模式构建和26个JS语法门禁需通过。
- 发布新增断言覆盖前后端／打包默认版本一致、运行时目录59项营养字段与已审核来源矩阵完全一致、必含0031／导入服务／目录JSON／数据声明。
- AFCD数据按专用许可分发适配条目，保留署名、完整限制说明、澳大利亚数据适用性、变更说明和许可链接；不以网站通用许可替代。详见backend/app/data/FOOD-DATA-NOTICES.md与逐项source_info。
- 隔离Agent的模型与worker关闭，其排队界面问题未作为Agent验收通过。正式app/config.py未被修改；不得把acceptance.py、局域网地址、测试账号或模型禁用环境变量带入正式包。
- 本轮不增加真实模型发布门禁。最终PR仍必须通过标准Project CI（含Python3.13容器构建），不得用本地测试代替远端CI。
- CI全绿且获得合并授权后，从合并后的干净main构建。核对前后端完整SHA、0.5.45、source_dirty=false及正式CloudBase配置；当前r2和分支预检构建都不是正式交付包。
- 保留0.5.44后端包和小程序dist，保留r2已验收隔离包及manifest；不覆盖用户已验收包。

## 用户部署顺序

本地最终验证已完成：后端1127 passed；Agent确定性39/39；小程序264项、Proposal88项、类型检查通过；隔离数据库0031→0023→0031成功；生产模式构建通过26个JS兼容门禁；凭据候选扫描和git diff检查通过。打包脚本PowerShell语法检查通过。预检包使用正式CloudBase环境与服务，未配置192.168局域网API；已有代码中的localhost开发备用常量仍存在，但CloudBase配置启用时不走HTTP分支。预检包仍为未提交分支构建，不作为正式交付。远端CI尚未执行。

1. 先确认生产数据库备份／恢复点可用，保留0.5.44回退包。当前未检查生产数据或备份。
2. 部署0.5.45后端；首次临时设RUN_DB_MIGRATIONS_ON_STARTUP=true执行0031，成功后恢复false。不盲目覆盖已有配置，不从隔离脚本复制Agent开关。
3. 核对迁移版本0031、/ready和后端构建身份；/ready现检查新增食品元数据列。导入采用稳定ID和台账，遇到同名不同定义会原子拒绝，不可绕过覆盖。
4. 后端确认正常后再导入正式小程序、预览验收及上传。验证分类／生熟搜索、每100g与kJ换算、保存草稿、旧快照和编辑按钮，以及原有训练／Agent功能。
5. 已修改或被餐次／Agent引用的新食品会阻止降级，禁止强行删除引用或清空用户数据。优先回退应用包并保留新schema；数据库降级仅在保护检查通过并另获授权时执行。

本文件不是上线完成记录，不授权跳过CI、自动生产迁移、上传或删除用户数据。
