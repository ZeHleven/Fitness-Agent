# 力量／核心动作库移植 · 2026-09-13

状态：代码移植和本地自动化完成；用户于2026-09-13回复“验收通过”，本轮隔离测试五组验收记为通过。随后用户授权进入下一步，现进入独立本地提交阶段；尚未推送、合并或发布。

## 分支和范围

- 基线 main：33a412a289a053d3b221463183973356c915d2ea；分支 codex/exercise-library-strength-core。
- 独立 worktree：.tmp/exercise-library-strength-core。外层旧开发分支及正式包保持不变。
- 按已验收 8653 独立预览接入：编辑计划和生成计划共用添加／替换弹层、七个部位＋全部、名称／别名搜索；保留草稿删除和自定义动作入口。
- 删除仅从当前草稿移除，允许暂时删空一天再添加；保存仍检查每个训练日至少一个动作。替换保留原组数、次数、间歇和位置。编辑计划仍须提案确认后才应用。
- 不新增有氧／等长计时或助力重量记录，不解禁原先不适合计次的动作，不修改历史训练，不增加动作详情页、视频或食品库功能。

## 动作数据

- 原种子20项不改名、不覆盖。新增48项；总标准条目68，通常可选力量／核心65项（17旧＋48新），实际按个人地点、健康和难度继续过滤。
- 数据：backend/app/data/exercise_catalog_v1.json。定义包括器械、肌群、运动模式、难度、计次说明和现有健康排除规则。
- 犀牛深蹲明确限定腰带式，不把其他类似器械强行当作同一动作；真机验收须核对用户所指器械。
- 髋内收归腿部，髋外展归臀部；复合动作可跨部位查询，只有一个动作ID。
- 旧仰卧起坐／Crunch名称不一致保留，不添加误导性的卷腹别名、不修改历史。
- 单侧动作采用每侧次数，左右均完成后记一组；这是本轮数据计数约定，不重解释旧记录。健康标签沿用产品的保守排除逻辑，不表示医学适用性认证。
- 元数据仅供选择展示及检索，不授予权限、不由客户端指定MET。标准动作沿用现有分类快照及力量能量估算。

## 数据迁移和兼容

- Alembic0030接0029，创建内部导入台账。稳定UUID、事务锁、完全一致定义才复用，不按同名覆盖，不重新启用旧动作。
- 检测到公共同名但定义不一致时整个升级拒绝，部署前须核对冲突；未查询生产库。
- 重复导入不重复创建，不覆盖后续更正。
- 降级只删除本迁移创建的且未修改、未被计划／历史／Agent JSON引用的条目；已有匹配条目不删除。已有引用时拒绝降级，不能为了回退应用强行丢数据。
- 公共GET /exercises增量支持q和body_part。两个计划上下文返回body_parts/search_aliases/counting_note；旧字段保留。
- 客户端搜索已获授权候选集，120ms合并输入，在页面内筛选，不逐动作或每次按键请求。仍由服务端复核保存候选，私有动作不进入公共搜索。

## 已执行验证

- 完整后端测试：1113通过（196.57秒），含新增59项目录、分类、搜索、迁移保护测试；现有依赖弃用警告未在本轮处理。
- 完整小程序测试：341通过；新增真实组件及页面集成覆盖搜索、筛选、无结果、清除、取消、重复／过期选择、键盘、后台、添加替换删除、失败保留和显式保存。
- TypeScript类型检查通过；原Proposal契约包含在完整测试中。
- Agent确定性评测39项通过，各主要准确率1.0、越权工具泄漏0；未调用真实模型。
- 专用空库实际upgrade head → downgrade0023 → upgrade head通过；另测引用／编辑／同名冲突时原子拒绝。
- 真实接口联调：专用合成账号两个计划上下文均返回65个允许选项及分类元数据；/ready、/test-info成功。
- 另建自动化专用账号实际创建钟摆深蹲替换提案，确认前卧推动作不变，确认后生效并保留3组／8次／90秒间歇；未操作用户验收账号的提案。
- 同机LAN的7次编辑上下文读取中位14.7ms、最大20.2ms；仅作本地样本，不代表生产网络或真机性能承诺。分类／搜索不增加网络请求。
- 微信生产模式编译通过，全包26个JS文件语法和原生导航契约通过。生产模式是编译优化，不代表正式发布包。
- 冻结测试包693791字节，参考正式0.5.43目录684768字节，增加9023字节（约1.32%，含测试项目配置差异）。
- 公共凭据扫描、git diff --check通过。远端Project CI与正式容器构建未在本轮执行；提交／发布另行授权。
- 自动化和编译不代替真机视觉、键盘、字体放大和网络恢复验收。

## 隔离验收交接

相邻目录 .tmp/exercise-library-test-20260913-r1/miniapp-dist。
测试项目 fitness-agent-exercise-library-test-r1，显示0.5.44 · 33a412a289a0 · dirty；0.5.44仅隔离包标记，本轮尚未更改正式版本号。
API http://192.168.1.21:8553/api/v1；test_id exercise-library-20260913-r1。
专用本地数据库 fitness_catalog_migration_20260913，自动化库 fitness_catalog_test_20260913，未接触原测试账号或生产数据。
该服务禁用模型调用、保留手动提案，使用单独合成账号（登录信息在隔离目录README，不进入公开源文件）。
保留8653参考预览、旧测试包和正式包。局域网变化须重核地址；禁止上传测试包。

## 真机五组

验收结果：用户已确认本轮通过；以下为对应验收范围。此确认不替代独立性能测量，也不构成提交或发布授权。

1. 编辑计划打开添加／替换，检查七个部位、名称／中英别名搜索、无结果和清除；核对新增机器动作和实际器械含义。
2. 添加一个动作、同日重复选择、替换、删除、删空后重新添加；替换原组数／次数／间歇不变，取消不更改。
3. 编辑目标次数后打开关闭弹层，切后台、键盘弹出、放大字体、窄屏和长动作名；无裁切、背景穿透或按钮遮挡。
4. 编辑计划只保存为待确认提案，确认前原计划不变；失败保留草稿，确认后按选择生效。自定义动作原入口仍可用。
5. 生成计划入口同样可筛选／搜索／添加／替换／删除，保留训练日选择；取消返回、断网恢复不误保存，不复发食物草稿／底栏回归。

真机满意后再决定独立提交和发布；本轮不把测试结果当作提交／上传授权。

## 本地提交交接

- 本次独立提交仅包含已验收动作库数据、0030迁移、查询元数据、共用选择组件、两个计划入口、相关测试及本记录。
- 不纳入测试账号、隔离服务脚本、构建包、日志或其他功能修改；不混入食品库扩充。
- 正式版本号尚未调整。下一阶段需另行授权推送／PR及0.5.44发布准备，标准Project CI通过后再决定合并与正式打包。
- 本轮包含后端变更与0030，不能像此前纯前端版本一样只上传小程序；正式交付时需先备份、部署后端及迁移，再验收和上传小程序。
