# 0.5.36 提交前检查与验收汇总

更新：2026-09-09。结论：本轮实现与用户验收完成，可以进入经授权的提交、推送及标准 Project CI 阶段；不是已经发布。

## 仓库与提交边界

- 仓库：`ZeHleven/Fitness-Agent`。
- 开发分支：`codex/nutrition-library-energy-0.5.36`。
- 当前 HEAD 是开发基线 `a2f7651ddd28629f1bc019efcfb294c58f966512`，不是新版本的正式发布提交。
- 本次只读查询远程确认 `main` 仍在该基线，尚无同名远程开发分支。不需要为同步新增 main 提交而变基。
- 后端与小程序版本均为 `0.5.36`；Alembic 唯一 head 是 `0029`，按顺序包含新增 `0028`、`0029`。
- 提交候选只包含本版 `backend/`、`miniapp/` 源码／测试和本版 `docs/` 记录。
- `.tmp/` 内隔离服务、测试数据库备份、局域网包、构建副本均仅保留本地。
  `deploy/cloudbase/0.5.34-7958ef2/`、`deploy/cloudbase/0.5.35-a2f7651/`
  是原回退资料，保留但不加入本次提交。环境配置、构建 dist、依赖目录不提交。
- 不执行 `git add .`；之后需要按候选文件明确暂存，再检查 staged diff。
- 本次检查没有暂存、提交、推送、创建 PR、合并、部署或上传；没有重启测试服务或改动验收账户数据。

## 已验收范围

原饮食食品库／能量概览七组模拟器验收已经记录在实施文档。本轮新增分类补全由用户在手机逐组确认：

1. 历史训练补充分类、重新进入后保留。
2. 新训练携带分类并参与当天消耗和差额。
3. 动作库分类仅影响今后训练，历史快照和对应消耗不被回写。
4. 本次训练分类更正可显式同步今后，不改其他历史；测试后恢复正确分类。
5. 饮食草稿、添加后自动定位、键盘、离页返回、断网及恢复网络。

第二组中发现“移除后重新加入周三继承旧完成状态”，已单独修复：保留连续训练日进度，只区分有明确移除证据的新安排。用户随后确认第二组通过。历史实际训练数据未删除或重写。

以上是用户反馈，不冒充代理亲自在真机操作验证。此前局域网地址变化和开发工具预览语法错误的修复过程保留在原实施记录中。

## 自动化与构建依据

- 最后一次业务代码修复后，Linux/Python 3.13 全后端 **823 项通过**，89.12 秒；包括迁移保护及训练／提案／能量测试。624 条已有 jose UTC 弃用警告，未隐藏失败。
- 小程序行为／构建契约 121、Proposal 契约 56，共 **177 项通过**；类型检查通过。
- Agent 确定性评测 **39/39**；没有新增真实模型发布门禁，也未调用真实模型。
- 本次提交前额外复建正式连接配置 QA 包：Webpack 37.51 秒成功，全部 **25 个 JS** 通过 ES2019 语法及产物契约。
- 编译产物中云传输配置、`fitness-agent-api` 服务名和 AppID 对应关系已核对；没有混入两个旧／当前局域网 `:8536` 测试地址。没有打印凭证或云环境原值。
- QA 包仍是 `0.5.36 / a2f7651ddd28629f1bc019efcfb294c58f966512 / dirty=true`，不能作为正式上传包。
- 提交候选使用仓库现有凭证扫描器的规则检查；扫描范围明确排除仅本地保留的临时与旧回退目录，不把它们视作已经批准发布。未发现凭证类模式；这不是完整安全审计。
- `git diff --check` 通过。提交前只整理文档状态和迁移说明，没有在验收后改动业务代码。

## 仍需完成的发布步骤

1. 经用户授权后明确暂存、提交、推送开发分支，运行标准 GitHub Project CI。当前远程 CI 尚未运行。
2. CI 全绿后经授权创建 PR、合并。从合并后的干净 main 构建正式后端包和小程序 dist，记录完整提交 SHA、版本及 `dirty=false`。
3. 保留旧版回退包。用户先备份线上数据库，再部署后端并迁移至 **0029**；核对 revision 与 `/ready`。
4. 启动迁移仅临时开启，确认完成后恢复 `RUN_DB_MIGRATIONS_ON_STARTUP=false` 并重启核对。
5. 使用正式小程序包做正式云传输／微信登录及关键路径验收，再由用户上传。局域网邮箱登录通过不能替代这一步。

不强制降级：0028 已有私人食品、0029 已有用户分类／修订时会拒绝丢弃数据。问题优先向前修复，需要恢复备份时另行核定。

## 后续体验事项（不在本轮继续扩展）

- “修改估算分类”目前在编辑计划的自定义动作展开区底部，功能已验收，但入口可发现性仍可优化。
- 手动计划提案的统一找回入口仍是已记录的后续事项，不能将曾用过的开发工具导航当作面向普通用户的完整入口。

本轮结论仅为可提交候选，不代表公网开放或所有体验／性能问题均已解决。
