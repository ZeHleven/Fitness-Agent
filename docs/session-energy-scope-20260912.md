# 本次训练估算分类 — 隔离移植 2026-09-12

基线：已验收加载优化本地提交 `f40b057d0c69f86ad89c28f08ca7cc2b3890f793`。
分支：`codex/session-energy-scope`。用户已授权本轮独立本地提交；不推送、不合并、不发布。

## 已验收的独立预览

`../energy-scope-preview-20260912-r1`，http://127.0.0.1:8647/ 。用户已经确认验收通过：

- 淡柠檬黄卡片，默认收起，点标题区域展开/收起，不增加展开按钮。
- 不显示两类动作的介绍段落。选项逐行排列，文案“抗阻训练”“自重抗阻”；保留安全边界说明。
- 点保存后询问“是否同步到这些动作以后的训练？”，主操作“仅当次”，次操作“同步”。关闭不保存。
- 浮层温和淡入及轻微位移，不弹跳、不缩放；使用原有深绿/米白色系。

## 实际移植

- 复用 WorkoutDisclosure，新增默认关闭的 headerOnly 可选参数；仅此带表单的卡片启用。既有动作卡片整卡点击行为不变。
- 保留原 API、所选行批量原子提交、训练及动作库 expected_version、响应不确定后的只读核对；没有引入新接口、迁移、MET 或计算规则。
- 分类值仍是 resistance_training / bodyweight_resistance。短标签仅用于本次训练编辑器，不扩大改动到其他动作库入口。
- RootPortal 自绘浮层，220ms 入场、160ms 退场；按 750 设计稿换算，入场位移约4px（375px屏）。页面隐藏立即关闭，不取消或重放请求；卸载清理计时器。
- 点范围按钮才写入；请求进行中阻止重复点击、取消和改选；未知结果禁止再次写入，保留“读取核对保存结果”。成功关闭浮层，但不强制收起卡片。
- 已停用动作仅支持本次修改。修改范围按实际所选行判断，未选到的停用动作不会阻止同步。
- 关闭图标来自 Radix Icons Cross2Icon（MIT，已补许可）；没有添加运行时依赖。

## 隔离测试包

导入相邻目录 `session-energy-scope-test-20260912-r1/miniapp-dist`，不要导入源码或正式目录。

- 项目名：`fitness-agent-energy-scope-test-r1`
- 版本：`v0.5.42 · f40b057d0c69 · dirty`。本轮为未发布增量，dirty 正常。
- API：`http://192.168.1.2:8541/api/v1`，当前 WLAN 地址，不是生产环境。
- 预期后端标识：`agent-acceptance-20260910`，沿用原合成测试账号与数据库。
- 不需要生产后端部署或数据库迁移。禁止上传此局域网包。
- 准备时 Docker 未启动，后续已恢复原本地测试环境；`/test-info` 与 `/ready` 核对正常，启动前后原测试业务数据摘要一致。没有清空、重建或连接生产库。

## 验证与验收

自动化证据位于 `miniapp/qa/`，被冻结进测试目录。真实 Taro H5 在一次性 Edge 中使用浏览器内 fixture 响应，只模拟写入，不调用真实账户接口。为对齐微信原生宿主，截图适配了 H5 原生 page 字体及 `disabled="false"` 属性选择器差异；截图不是微信真机性能验收。

- 小程序完整行为测试：318/318 通过；TypeScript 类型检查通过。
- Taro H5 模拟交互检查：14 项通过，包括默认收起、取消无写入、当前训练批量保存、响应丢失后的只读核对、放大字体及关闭按钮位置。2 次写入均为浏览器内存 fixture，真实服务器写入为零。
- 微信生产模式编译及全包语法契约通过：25 个 JavaScript 文件。测试地址不代表正式发布配置。
- H5 编译保留现有 app/entry 体积警告；本轮未增加运行时依赖。后端代码未修改，未宣称重跑完整后端测试。

2026-09-12 用户明确反馈“这五组验收都已通过”。以下为用户报告通过的验收范围（非代理代替用户完成真机操作）：

1. 本次训练黄色卡片默认收起；点击平滑展开，两类选项分行、没有介绍段落；改选或滚动不会误收起，收起重开选择保留。
2. 选择分类点保存，出现温和浮层但还没有写入；关闭或点遮罩保留选择。
3. “仅当次”只更新本次；显式“同步”才影响今后分类，不改其他历史。返回饮食后刷新估算，不覆盖未保存餐次草稿。
4. 重复点击只提交一次；断网保留选择，响应不确定时只能先读取核对，联网恢复后不重复写入。
5. 已停用动作禁用同步；切后台返回无遗留浮层；放大字体可滚动、按钮文字不裁切；既有动作卡片展开收起正常。

验收前置条件：卡片仅对已结束训练中实际保存过有效组的本人自定义动作显示。曾只读核对到测试账号的自定义动作是零组，故当时不显示；已向用户说明先补充符合条件的测试记录，再验收。

本轮只做独立本地提交。验收包保持原文件、原 SHA 标识和 dirty 状态，不覆盖为新提交产物，不作为正式发行。后续发布仍需另行授权。

## 后续交接

- 本轮与此前加载优化保留独立提交，方便分别保留或回退；不要通过重置或覆盖方式撤销其他工作。
- 下一设计事项为底部“训练 / 饮食 / Agent / 我的”图标加文字导航。先选参考并制作不连接后端的独立预览，用户确认后才实施；保持深绿、米白和淡柠檬黄体系，不将参考的黑色背景直接套入。
- 目前未开始导航实现，也未授权本分支推送、合并或生产发布。
