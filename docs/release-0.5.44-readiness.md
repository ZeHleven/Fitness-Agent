# 0.5.44 发布准备：力量／核心动作库

日期：2026-09-13。用户已确认隔离测试五组通过，并授权分支推送、创建PR与0.5.44发布准备；合并待CI全绿后确认。部署后端和上传小程序由用户手动执行。

## 版本与范围

- main基线：33a412a289a053d3b221463183973356c915d2ea。
- 功能提交：b3879f4c26d76d1fc5ed74803cf1b5e09ed98308。
- 后端0.5.41 → 0.5.44，小程序0.5.43 → 0.5.44，数据库0029 → 0030。
- 新增48个力量／核心动作；保留旧20条原始种子数据。通常可选项为65个，实际受已有资料、健康、地点和难度过滤。
- 两个计划入口共用部位／名称／别名选择器，支持添加、替换、草稿删除；保留自定义动作和Proposal确认。
- 不扩展食品库、有氧／等长计时记录、助力重量语义、模型配置或意图链路，不改历史记录与消耗公式。
- 保留已验收隔离包，不用新构建覆盖。LAN包不得上传正式环境。

## 发布门禁

- 已验收版本：后端1113、小程序341、Agent确定性评测39项通过；类型检查、26个JS全包语法及导航契约、迁移升降级及引用保护通过。
- 发布准备增加前后端版本／后端包必含数据文件回归；标准Project CI必须对最终PR提交执行后端全测、迁移回环、Agent确定性评测、容器构建、小程序契约、类型和构建。
- 新增动作会影响候选池，但不改模型或生成链路；本轮不新增真实模型调用发布门禁。
- 后端打包要求0030、固定目录JSON、导入服务及查询元数据服务存在；不得夹带测试账号、.env、日志或本地数据库。
- 合并获得授权且CI通过后，从与origin/main一致的干净提交新建正式交付目录，构建前后端0.5.44，核对完整SHA、source_dirty=false、微信AppID、正式云环境和urlCheck=true。
- PR／CI／合并SHA／产物哈希及用户部署核对结果记录在交付目录；准备完成不等于已经上线。

## 部署顺序与回退

1. 用户先核对生产备份或可恢复点，并保留旧后端0.5.41与小程序0.5.43正式包。
2. 先部署后端0.5.44并运行0030。新数据采用稳定ID和导入台账；已有同名但不同定义的公共条目会原子拒绝，不能覆盖绕过；目前尚未查询生产库。
3. 核对数据库版本0030、/ready和后端构建身份。启动迁移开关仅首次临时开启，成功后恢复关闭。
4. 再导入正式小程序目录，预览验收后由用户上传。不能先上传依赖新候选元数据的小程序。
5. 已被训练／计划／Agent引用或被更改的导入动作会阻止数据库降级；优先回退应用包，不强行删除新记录、重置数据库或丢弃用户数据。
6. 正式验证至少核对部位搜索、两入口增删替换、提案确认、旧训练与饮食显示、断网草稿、手机键盘和导航。

本文件不授权自动部署、自动上传、数据库重置或跳过分支保护。
