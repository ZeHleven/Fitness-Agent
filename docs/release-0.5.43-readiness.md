# 小程序 0.5.43 发布范围与门禁

日期：2026-09-12。用户已确认三轮隔离验收，并授权推进提交、PR、CI、合并及正式构建；部署与小程序上传仍由用户操作。

## 范围

- 正式基线 main：`3d8d94eec0568231f8a9736385723d678c8a16f7`。包含 `f40b057` 加载缓存与文字等待反馈、`fad8b86` 估算分类卡片与范围确认浮层、`a445f9e` 紧凑胶囊导航与训练/饮食滚动渐隐。
- 小程序由 0.5.42 升至 0.5.43；后端仍为 0.5.41，数据库仍为 0029，无后端部署或迁移。
- 保留深绿、米白与淡柠檬黄，保留原生 tab 路由、草稿、分类版本与保存语义。Agent 聚焦输入框时隐藏导航经用户手机验证符合预期，不额外改动。
- 不变更模型、意图、Proposal 授权、训练计时、热量计算或公共食品/动作数据；不加入新的运行时依赖。

## 验证与发布

- 各轮验收记录分别见 loading-optimization-20260911.md、session-energy-scope-20260912.md、capsule-navigation-20260912.md。最终 r4 五组手机验收由用户确认全部通过，不扩大为所有设备性能测试通过。
- 本地完整小程序 334 项测试与类型检查通过。已有 r4 微信 26 文件全包语法/原生导航契约检查、90 项 H5 合成数据检查通过；H5 证据不冒充微信真机截图或 FPS。
- 发布前重新检查源码凭证与版本；标准 Project CI 后端测试/迁移/确定性评测/容器构建及小程序契约/类型/生产构建必须成功。不新增真实模型门禁，不跳过保护规则。
- 从合并后与 origin/main 一致的干净提交构建正式包，校验完整 SHA、0.5.43、source_dirty=false、云托管连接与合法域名校验。保留旧正式包及隔离包，新建交付目录，禁止上传任何 LAN 包。
- PR、CI、合并 SHA、正式包文件哈希与只读健康结果写入产物旁发布记录。正式包仍须用户预览验收，再手动上传；上传不代表审核或公开上线。
